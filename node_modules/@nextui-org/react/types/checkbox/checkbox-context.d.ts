import React from 'react';
import { CheckboxGroupState } from '@react-stately/checkbox';
import { NormalColors, NormalSizes, SimpleColors } from '../utils/prop-types';
export interface CheckboxConfig extends CheckboxGroupState {
    color: NormalColors;
    size: NormalSizes;
    labelColor: SimpleColors;
    inGroup: boolean;
}
export declare const CheckboxContext: React.Context<CheckboxConfig | null>;
export declare const useCheckbox: () => CheckboxConfig | null;
